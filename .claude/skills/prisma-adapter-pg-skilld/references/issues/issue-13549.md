---
number: 13549
title: Run migrations by code
type: question
state: open
created: 2022-05-29
url: "https://github.com/prisma/prisma/issues/13549"
reactions: 321
comments: 46
labels: "[kind/feature, topic: migrate, topic: sdk, topic: prisma migrate deploy, topic: programmatic usage]"
---

# Run migrations by code

I have a project with a main database, called general and inside it a table of companies, each company has a different database.
My problem is the following, in this project I use the typeorm, but I am considering migrating to the prisma, the problem is that in the typeorm I can run the migrations inside the connection.

```typescript
await connection.runMigrations({
       transaction: 'all',
});
```

Is there any way to get it to run in the same way as typeorm here in prisma??


I can connect to another db with a custom url:
```ts
import { PrismaClient } from '@prisma/client'

const client1 = new PrismaClient({ datasources: { db: { url: 'postgres://localhost/db1' }} })
```
https://github.com/prisma/prisma/issues/2443#issuecomment-630679118


---

## Top Comments

**@dongyuwei** (+29):

we do it this way:


```
import { execSync } from 'child_process';
execSync(`pnpx prisma migrate deploy --schema db/shared/shared.prisma`, { stdio: 'inherit' });
```

If your prisma shema need some environment variables, just pass it to the cmd, eg :

```
import { execSync } from 'child_process';
execSync(
    `export DATABASE_URL=${config.database.url}?schema=${tenantId} && pnpx prisma migrate deploy --schema db/tenants/tenants.prisma`,
    { stdio: 'inherit' },
  );
```

**@janpio** (+11):

What exactly do you want to do? Run migrations via code on the same connection you use to run queries against your database? Would `migrate deploy` and similar not work for you?

**@janpio** (+4):

Currently that is not easily possible. The best approach here would probably be to script the usage of `prisma db push` or `prisma migrate deploy` and modifying the environment variable that holds the connection string each time.