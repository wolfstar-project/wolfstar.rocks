---
number: 37739
title: Unable to use playwright Agents in Cursor
type: other
state: closed
created: 2025-10-07
url: "https://github.com/microsoft/playwright/issues/37739"
reactions: 40
comments: 13
---

# Unable to use playwright Agents in Cursor

Unable to use playwright Agents in Cursor

---

## Top Comments

**@gustavo-meilus** (+5):

Found a workaround that actually works.

Tests fail to load with those annoying "Playwright Test did not expect test() to be called here" errors because it's starting from the wrong directory.

I just wrapped it in a shell script that explicitly sets the working directory first.
Create ~/.cursor/playwright-test-wrapper.sh:
```
#!/bin/bash
cd /path/to/your/projectexec npx playwright run-test-mcp-server --config playwright.config.ts "$@"
```
Make it executable:
```
chmod +x ~/.cursor/playwright-test-wrapper.sh
```
Update your ~/.cursor/mcp.json:
```
{
  "mcpServers": {
    "playwright-test": {
      "type": "stdio",
      "command": "/home/user/.cursor/playwright-test-wrapper.sh"
    }
  }
}
```...

**@gustavo-meilus** (+5):

I was trying to do a workaround to use the Agents on Cursor and I managed to add the Agents manually on it:

<img width="262" height="120" alt="Image" src="https://github.com/user-attachments/assets/cd65510a-0323-4a57-b32d-07a8da9610b6" />

<img width="618" height="100" alt="Image" src="https://github.com/user-attachments/assets/02039ec2-4f10-4e71-b052-7269be69a8ce" />

But I end up getting this error when I try to use it, it seems that is an issue when executing the MCP from Cursor, there's a way to solve this?

...

**@igo0r** (+1):

With the latest Cursor I was able to use it as is:
https://www.youtube.com/watch?v=LsDS0Z6E4NM