name: Code Scanning

on:
  push:
    branches:
      # for now is set to refactor/imagine-an-app
      - refactor/imagine-an-app
  pull_request:

jobs:
  CodeQL:
    name: Analysis
    uses: sapphiredev/.github/.github/workflows/reusable-codeql.yml@main
    with:
      with-auto-build: false
