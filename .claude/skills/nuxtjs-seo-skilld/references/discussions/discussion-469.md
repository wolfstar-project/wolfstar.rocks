---
number: 469
title: "I don't understand if the sitemap with `nuxt build` (not generate) and `crawlLinks: true` should automatically add dynamic pre rendered pages or not."
category: "Q&A"
created: 2025-08-12
url: "https://github.com/harlan-zw/nuxt-seo/discussions/469"
upvotes: 1
comments: 1
answered: false
---

# I don't understand if the sitemap with `nuxt build` (not generate) and `crawlLinks: true` should automatically add dynamic pre rendered pages or not.

I don't understand if the sitemap with `nuxt build` (not generate) and `crawlLinks: true` should automatically add dynamic pre rendered pages or not.

---

## Top Comments

**@harlan-zw** [maintainer]:

If the links are discoverable during the crawl then they should be added to sitemap, feel free to make an issue with reproduction if that's not the case :) 