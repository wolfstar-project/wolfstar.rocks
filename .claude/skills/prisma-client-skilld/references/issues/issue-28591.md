---
number: 28591
title: Mapped enums with create function seem to not work correctly
type: bug
state: closed
created: 2025-11-20
url: "https://github.com/prisma/prisma/issues/28591"
reactions: 38
comments: 43
labels: "[bug/2-confirmed, kind/bug, tracked]"
---

# Mapped enums with create function seem to not work correctly

### Bug description

I have a mapped enum in my schema, something along the lines of:
```
enum SuggestionStatus {
  PENDING  @map("pending")
  ACCEPTED @map("accepted")
  REJECTED @map("rejected")

  @@map("SuggestionStatus")
}
```
And a create request for it in a test:
```
const enrichment = await prisma.suggestionModel.create({
      data: {
        ...
        status: SuggestionStatus.PENDING,
      },
});
```
I'm getting the following error:
```
PrismaClientValidationError: 
    Invalid `prisma.suggestionModel.create()` invocation in ...
    ...
      data: "pending"
    ...
    Invalid value for argument `status`. Expected SuggestionStatus
```
If you have any insight I would appreciate it.

### Severity

 Minor: Unexpected behavior, but does not block development

### Reproduction




### Expected vs. Actual Behavior

I expected the above syntax to work, since this matches the requirement from the typing system.

### Frequency

Consistently reproducible

### Does this occur in development or production?

production

### Is this a regression?




### Workaround

Mapping to name in the code using a reverse mapping with a type cast.


### Prisma Schema & Queries

```prisma
enum SuggestionStatus {
  PENDING  @map("pending")
  ACCEPTED @map("accepted")
  REJECTED @map("rejected")

  @@map("SuggestionStatus")
}

model SuggestionModel {
  id                 Int              @id @default(autoincrement())
  suggestedContent   String       x    @map("suggested_content")
  status             SuggestionStatus @default(PENDING)

  @@index([status])
  @@map("snippet_suggestions")
}

```
```ts
// Add relevant Prisma Client query
```


### Prisma Config

```ts
// This file was generated by Prisma and assumes you have installed the following:
// npm install --save-dev prisma dotenv
import "dotenv/config";
import { defineConfig, env } from "prisma/config";

...

---

## Top Comments

**@leo-ease** (+14):

Same issue here. I can't imagine this being the intended behavior, because you can no longer even use the generated enum type as a value.  

The Typescript typing shows the create function expecting the mapped value, but then that leads to a runtime error when you use it (whether as a literal, or by referencing the generated enum- e.g. `SuggestionStatus.PENDING`). 

For it to work, you have to use the string key of the enum (the non-mapped value- e.g., "PENDING"), which seems totally backwards and violates the generated type.

...

**@mario-jerkovic** (+8):

> [@mario-jerkovic](https://github.com/mario-jerkovic), what you're describing is the previous behaviour of the enums. Now that the mapped value is exposed in the TypeScript enum it will actually be visible in the application.
> 
> The breaking change will either need to be reverted, so it's hiding the value, or it should be used as input and output values too. Fully exposed. I don't think we can have both...
> 
> It's a major oversight from the prisma team to not have tested it as an input/output value again TBH. The generated enum is not useable in it's current form.
> 
> Edit: Personally I ...

**@mario-jerkovic** (+6):

I’d like to add my perspective because I’m running into the same problem, and I think the core issue is that the current behavior breaks the expected mental model of how enum `@map` should work.

## Expected behavior

Given an enum like:

```prisma
enum Color {
  Light @map("LIGHT")
  Dark  @map("DARK")
}
```

Prisma should treat the **schema enum names** (`Light`, `Dark`) as the only values my application code ever interacts with, while Prisma internally maps them to/from the database values (`LIGHT`, `DARK`).

## Write example

...