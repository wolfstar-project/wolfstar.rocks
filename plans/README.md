# Implementation Plans

Generated by the `improve` skill on 2026-07-04 at commit `d9a206c6`. Execute in
the order below unless dependencies say otherwise. Each executor must read its
plan fully, honor its STOP conditions, and update its row when done.

## Execution order and status

| Plan | Title                                                   | Priority | Effort | Depends on | Status |
| ---- | ------------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Enforce typechecking in CI                              | P1       | S      | —          | TODO   |
| 002  | Exercise real security boundaries in tests              | P1       | M      | 001        | TODO   |
| 003  | Validate OAuth state before session creation            | P1       | M      | 002        | TODO   |
| 004  | Authorize every cached guild-log request                | P1       | M      | 002        | TODO   |
| 005  | Make rate-limit reservations atomic                     | P1       | M      | 002        | TODO   |
| 006  | Make the audit verifier reproduce the persisted chain   | P1       | M      | 001        | TODO   |
| 007  | Honor the configured Sentry trace sample rate           | P2       | S      | 001        | TODO   |
| 008  | Persist compact guild-settings audit changes            | P2       | M      | 006        | TODO   |
| 009  | Remove production `any` from guild-settings forms       | P2       | L      | 001        | TODO   |
| 010  | Reconcile contributor documentation with the repository | P3       | S      | 001        | TODO   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with a one-line reason) |
REJECTED (with a one-line rationale).

## Dependency notes

- 001 lands first so every later change has an enforced strict-TypeScript gate.
- 002 characterizes the real OAuth, cache, and rate-limit wrappers before
  003–005 alter them.
- 006 precedes 008 so the verifier supports the existing envelope before its
  `changes` payload evolves.
- 009 follows the security work to avoid mixing a broad type refactor into
  urgent boundary fixes.

Plans 003, 004, and 005 can run in parallel after 002. Plans 006 and 007 can run
in parallel after 001.

## Shared final verification

Before committing any plan, run the repository-mandated gates in this order:

1. `pnpm build` — exit 0.
2. `pnpm lint:fix` — exit 0; review formatter changes and keep only in-scope
   files.
3. `pnpm typecheck` — exit 0 with no errors.
4. `pnpm test` — all Vitest projects pass.

Use Conventional Commits: `<type>(<scope>): <subject>`. Do not push or open a
pull request unless explicitly instructed.

## Findings considered and rejected

- `v-html` in `app/components/command/Base.vue`: rejected as an XSS finding
  because values pass through the local escaping/formatting function before
  rendering.
- Missing guild authorization: rejected because every audited guild route uses
  `auth: true` and calls `canManage()` before returning privileged data.
- Console output in scripts and drain-failure paths: intentional operational
  output, not application logging spam.
- `service-worker/tsconfig.tsbuildinfo` committed to git: real repository noise
  but too little user or maintenance impact to justify a standalone
  implementation plan.
- Dependency advisories: not assessed. The registry audit required permission to
  transmit dependency metadata externally and was not authorized during this
  audit.
